<%= simple_form_for(@cocktail) do |c| %>
  <%= c.input :name %>
  <%= c.input :description %>
  <%= c.input :photo %>
  <%= c.input :photo_cache, as: :hidden %>
  <%= c.submit "Submit", class: "btn btn-default" %>
<% end %>
